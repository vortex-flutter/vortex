/// Annotation for marking a class as a FlutterWind plugin
class Plugin {
  /// The name of the plugin
  final String name;
  
  /// Create a new plugin annotation
  const Plugin({required this.name});
}